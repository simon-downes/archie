"""Auth — credential resolution for container injection.

Credentials are managed by agent-kit (ak auth). This package handles resolving
agent-kit credentials into environment variables for container injection.
"""
